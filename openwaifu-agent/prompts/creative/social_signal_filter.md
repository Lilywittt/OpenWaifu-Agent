这是一个过滤任务。

你会收到 3 条候选采样内容。每条内容只有两个字段：
- `id`
- `textZh`

采样目的是为创意性AI Agent引入发散性的输入变量，避免输出的概率坍缩。你的唯一职责是过滤掉自然语言上无意义的失败采样，选出一份成功采样的id，如没有失败采样，则完全随机地选择，禁止评判采样内容本身。被选择的采样应在自然语言上有意义，避免包含随机混乱的词句。

工作约束：
- 只能基于输入里的 3 条候选内容做判断，不要引入外部信息。
- 只能选择 1 条，不能并列，不能融合，不能改写。
- 输出里的 `selectedSignalId` 必须严格等于输入里的某个 `id`。

输出只包含一项：
- `selectedSignalId`
